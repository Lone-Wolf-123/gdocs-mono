// src/types/prisma-shim.d.ts

declare module '@prisma/client/runtime/library' {
  // This empty declaration satisfies the TypeScript compiler, telling it
  // that this module exists and can be imported (for type purposes).
}
